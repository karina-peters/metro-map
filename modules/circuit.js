export class Circuit {
    constructor(id, sequence, station, mapX, mapY) {
      this.id = id;
      this.sequence = sequence;
      this.station = station;
      this.mapX = mapX;
      this.mapY = mapY;
    }
}